# 04 · 颜色语义 · 安全符号 · 描述染色与星级方法

> loot 简称染色、tooltip 美化、百科排版的通用规范。具体配色/星级分档是**设计选择**，可按项目调整；本篇给的是经过验证的可复用范式。

---

## 1. D2R 文本颜色控制码

格式：`ÿc` + **单个控制字符**。高亮后**行尾必须 `ÿc0` 复位**，防止颜色泄露到后续行。
（`ÿ` = U+00FF，脚本里写 `\xffc4` 这种。）

| 码 | 当前 DLC 色值/颜色 | 常用语义 |
| --- | --- | --- |
| `ÿc0` | `#ffffff` 白 | 默认 / 复位闭合 |
| `ÿc1` | `#ff5151` 红 | 火焰、生命、治疗药水 |
| `ÿc2` | `#01ff01` 亮绿 | 毒素、**套装装备** |
| `ÿc3` / `ÿcB` | 蓝紫档（见色卡） | 法力、魔法装备、描述上下文复位色 |
| `ÿc4` / `ÿc>` / `ÿcD` | `#c9b57a` 暗金 | 技能加成、完美宝石、暗金物品 |
| `ÿc5` | `#6d6d6d` 灰 | 杂项/旧式描述色（建设中建议迁移到语义色） |
| `ÿc6` | `#010101` 黑 | 极少使用 |
| `ÿc7` | `#d1c480` 淡金 | MF/GF、低级宝石/骷髅 |
| `ÿc8` / `ÿc@` / `ÿcJ` / `ÿcL` | `#ffaa01` 橙 | 符文(统一)、增伤/攻击特效、重置材料 |
| `ÿc9` / `ÿc?` | `#ffff68` 亮黄 | 闪电、速度(FCR/FHR/IAS)、命中 |
| `ÿc:` / `ÿcA` | `#018301` 深绿 | 属性族、备选绿 |
| `ÿc;` | `#b001ff` 紫 | 魔法/全抗、紫宝石、回复药水 |
| `ÿc<` | `#01ca01` 绿 | 备选绿 |
| `ÿcC` | `#01fc01` 亮绿 | 备选绿 |
| `ÿcE` / `ÿcF` / `ÿcG` / `ÿcH` / `ÿc=` | `#f0f0f0` 近白 | 轻提示，不建议大量用 |
| `ÿcI` / `ÿcK` | `#676767` 灰 | 旧表常误记，按当前色卡为准 |
| `ÿcM` | `#9f4ffc` 紫（DLC 新增/确认） | 魔法/稀有强调可选 |
| `ÿcO` | `#08a8de` 青蓝（DLC 变化） | 水/冰/提示色可选 |
| `ÿcP` | `#ff83ff` 粉紫（DLC 变化） | 防御·格挡族等语义色 |
| `ÿcQ` | `#acacff` 淡蓝紫（DLC 变化） | 备选冷色 |
| `ÿcS` | `#ffff79` 亮黄（DLC 变化） | 高亮提示 |
| `ÿcT` | `#d14242` 暗红（DLC 变化） | 生命·治愈·吸血族 |
| `ÿcU` | `#85c7ff` 浅蓝（DLC 变化） | 冰系全族 |
| `ÿcV` | `#ff0101` 红（DLC 变化） | 强警示；旧“白框禁用”结论已作废 |

> ⚠️ **DLC 色码铁律（2026-06-13 色卡截图校正）：颜色是 DLC/客户端标准，不是社区旧表。** 旧结论“只有大写 A–U 支持、V 以后白方块”在当前 DLC 色卡下已经作废；`ÿcV` 现在是红色，`ÿcM/O/P/Q/S/T/U` 等也有明确变化。截图未覆盖或看不清的码，不要凭旧表补。
>
> **重要：颜色必须以游戏内色卡为准。** 不同客户端版本/语言/平台可能改变渲染。工程规范：移植色码诊断面板 → 进游戏截图 → 对照截图记录色值 → 更新到项目标准文档。社区表、旧 guide、其他 mod 只能当历史旁证。
>
> 一致性提醒：**套装=`ÿc2`(绿)**，不是 ÿc5；同一项目内**符文建议统一一色**（如 `ÿc8` 橙），别多色打架。
> 易混色警告：**`ÿc7`(暗金) 与 `ÿc4`(暗金) 同色系**，地面标签几乎分不出来——黄色系物品别用 ÿc7，用 `ÿc9` 亮黄（主流 loot filter 同款）。

### 1.0 DLC 扩展色码：以色卡截图定案（2026-06）

**实测结论（2026-06-13 DLC 色卡截图为证）**：

- 颜色问题不是“随机变化”，而是 **DLC 后标准改变**。旧社区表和旧 guide 都不能继续当权威。
- `ÿcM` 有 `NEW` 标记；`ÿcO/P/Q/S/T/U/V` 等有 `CHANGE` 标记。使用这些码时，直接按当前色卡记录语义。
- `ÿcI/ÿcK` 当前为灰；`ÿcE/F/G/H/=` 当前为近白；`ÿc@/J/L` 与 `ÿc8` 同橙；`ÿc?` 与 `ÿc9` 同亮黄。
- vanilla 提取件中官方自己的用法可作旁证，但**必须 grep 原版提取件**；不要信继承自其他 mod 的工作目录。

铁律：
- **色码只信游戏内色卡截图**。把色码面板字符串（数字+符号+全字母遍历实时渲染）移植进目标客户端打开，全部一次出答案。
- guide 只能记录“某次截图确认的当前标准”；项目上生产前以自己的客户端截图复核。
- 未被色卡覆盖的码点按“未验证”处理，不要补旧社区结论。

### 1.1 多行名字串渗色（"暗金变黄/变灰"的根因，铁律级）

D2R 的 TooltipStyle 默认 `"newlineHandling":"reversed"`——**多行名字串（注解行/loot filter 装饰行）反序渲染**。后果：注解行**行尾悬空的色码会渗染进名字行**：行尾悬空 `ÿc9` → 暗金名变黄；悬空 `ÿc5` → 名字变灰。

**铁律（成熟 loot filter 全部这么写）**：
1. **每一行行首显式自带色码**，永不依赖上一行或品质色继承。多行的暗金名给名字行加 `ÿc4` 前缀、套装加 `ÿc2`（与品质色相同，视觉无变化，但渲染从此确定）。
2. **串尾 `ÿc0` 封口**，杜绝串内/串间渗色。
3. 底材（颜色随 白/超强/无形 变化的）**不要加品质前缀**，只封口——硬加会盖掉引擎品质色。

**机制补充（两家成熟 mod 实证，2026-06）**：`reversed` 下渲染遍历方向 = **串尾行→串首行**，所以"串中靠后的行"的行尾悬空色码才会渗进名字行。防渗色有两个等价流派，任选其一并全串贯彻：

| 流派 | 写法 | 实证 |
| --- | --- | --- |
| **显式色码流**（MDK） | 每行行首显式色码，基本不封口 | MDK item-names 1574 条只有 15 条用 `ÿc0` 封口 |
| **零色码流**（ComG） | 名称/信息行**完全不写色码**→整串继承引擎品质色，天然零渗色；仅个别染色条目（宝石）才用色码且**必封口** | ComG item-names 1660 条仅 34 条含色码，34/34 全部 `ÿc0` 封口 |

> 零色码流是更简单安全的默认；只在确实要改色的条目上引入色码，且引入即封口。

**`|` 管道符 = 行分隔**：`TooltipStyle.options.pipeCharacterBehavior:"handle"`（两家 mod 均开启）下，名字串里的 `|` 等价 `\n`。MDK 底材串 `╮连枷 Flail|普【④:Lv26-40】` 即此写法。

### 1.2 符文双 key：r## 与 r##L（装饰只进拾取、不污染显示）

`item-runes.json` 里每个符文有**两组 key**，用途不同：

| key | 用在哪 | loot filter 用法 |
| --- | --- | --- |
| `r01`–`r33` | **地面拾取标签** + 背包物品名 | 装饰框/星星/出货横幅放这里 |
| `r01L`–`r33L` | **镶嵌列表 / 符文之语 tooltip 列出的符文名** | **必须保持单行干净名**（如 `ÿc4乔ÿc5Jah ÿc2#31`） |

把 `r##L` 一起装饰 = 做好的符文之语 tooltip 里满屏星星。主流 loot filter（MDK 等）全部是 r## 装饰、r##L 干净的双 key 分离写法。

### 1.3 名称显示范式实证库（ComG 极简流 vs MDK 全家桶流，2026-06 抽样）

两家成熟 mod 的名称写法逐类对照，做 loot filter 时按口味取用：

| 内容 | ComG（韩服皮肤包，极简） | MDK（全功能） |
| --- | --- | --- |
| **底材名** | `(防御90-107)\n名称¹③` —— 信息行(纯文本括号)+名称行；档次=上标`¹²³`(普/扩/精)，最大孔数=带圈数字`③⑤`；**全程零色码** | `图标+中文名 English\|普【④:Lv26-40】图标` —— `\|`分行；档次=单字`普/扩/精`；`【孔数:适用Lv段】`；底材用途标签`【野兽】【CTA】`(符文之语底材提示) |
| **底材信息行内容** | 防御/伤害数值范围；杖类写自动词缀(staffmod)提示`(天堂之拳or祝福之锤/…)` | 拆进标签系统(见上) |
| **宝石** | 等级用词头（最下级/下级/无/上级/最上级）+ 种类词染元素色 + `ÿc0` 封口，单行：`상급 ÿc;자수정ÿc0` | 元素色+图标分级`┥┦┧┨┩`(1-5级各一图标)+等级上标`¹²³⁴⁵`：`ÿc;┨无瑕紫宝石⁴` |
| **普通符文 r##** | 编号前缀：`13.샤엘 룬`（无色码） | 图标+名+灰英文+绿编号：`┄符文：那夫ÿc5Nef ÿc2#4` |
| **高阶符文 r##(26+)** | 彩条横幅：`ÿc1***ÿc8***ÿc9***ÿc2***ÿc3***ÿc;***\n\nÿc8NN.名 룬\n\n彩条…ÿc8` | 多空行撑高+横幅文案`=====【SSR 出货啦！】=====` |
| **r##L** | 纯名无码：`샤엘` | 单行带色码：`ÿc4萨德ÿc5Zod ÿc2#33`（单行干净=无换行无装饰，色码允许） |
| **符文之语名** | 纯文本不装饰 | `ÿc4中文名 ÿcI英文名ÿc8` |
| **任务/特殊物品** | 不特别处理 | 名称行+分类标签行：`ÿc8【任务物品】``ÿc1『A1女伯爵掉落,地狱火炬道具』` |
| **隐藏垃圾物品** | （用 lootfilter 面板） | 名称替换为 `U+E01A` 字符×41 + `[屏蔽]`——用占宽字符把文字推出可读区 |

其他可复用细节：
- **名称组装格式 key**（`item-nameaffixes` 的 `MagicFormat`/`HiqualityFormat` 等）：用 `%0 %1 %2` 占位符可重排"前缀/底材/后缀"语序、甚至拆两行（MDK：`ÿc3%1\nÿc3『 %2 ÿc3之 %0 ÿc3』`——魔法装备词缀单独成行加书名号）。
- **修饰行（item-modifiers）的行内复位色是 `ÿc3` 不是 `ÿc0`**：描述上下文默认蓝，行内插彩色片段后应复位 `ÿc3`（MDK：`镶孔 (ÿc8%iÿc3)`）；`ÿc0` 在 tooltip 描述里会复位成白。
- MDK 把同一文件做成多个预设变体（带英文/不带英文/单行版；全彩/少彩/无彩），构建时按用户配置选装——做可配置 mod 的参考结构。

---

## 2. 字体安全符号库

只有字体里有字形的符号才能用，否则空白方块 `☐`。以下为常见**已验证安全**集（仍以你项目字体的覆盖检查为准）：

- 品质/层级：`①②③`(U+2460+)、`★`(U+2605)、`◇`(U+25C7)、`▽`(U+25BD)
- 宝石/骷髅前缀：`ↀ`(U+21A1)、`ↂ`(U+21A2)、`₾`(U+20BE)
- 排版：`×`(U+00D7 乘号，**禁用字母 x**)、`→`(U+2192 箭头，**禁用 `->`**)、`·`(U+00B7 中点)

> ⚠️ `▪`(U+25AA) 在部分 D2R 字体显示为空白，改用 `▽`。**用任何新符号前先跑字体覆盖检查**（见 `tools/`）。

### 2.1 覆盖检查通过 ≠ 字形非空（踩坑实证）

字体 cmap 里**有码点映射**不代表**字形有内容**——空字形（zero-contour glyph）会通过一切"缺字检查"，游戏里却什么都不显示（连方块都没有）。实证：某私有区码点 cmap 检查通过，进游戏前缀直接消失。防坑三步：

1. 新符号优先选**已知图标区段**（你字体里确认有内容的区间），不要随手挑"看起来没人用"的码点；
2. 用 fontTools 查该码点字形的 `numberOfContours`/宽度（图标字形通常宽度=UPM 正方形），**空字形=0 轮廓**；
3. 最终以游戏内截图为准——覆盖脚本只能保证"不缺字"，保证不了"能看见"。

### 2.2-pre `\ue0XX` 图片字形：手柄图标、effecticon 槽位、字体兜底要分开

除字体字形外，D2R 对一部分 **U+E0xx 私用区字符**会走图片替换层，渲染成全彩 sprite。它与 TTF 的普通轮廓字形是两条不同管线：

```
字符串写 U+E0xx
  → 引擎命中图片槽位：渲染全彩 sprite，不吃 ÿc，天然零渗色
  → 未命中图片槽位：回落到字体里的单色兜底 glyph，吃 ÿc
```

这件事有两个常被混淆的分支：

| 范围 | 载体 | 是否能由 mod 改图 | 说明 |
| --- | --- | --- | --- |
| `E000`–`E030` 附近 | 原版手柄/系统提示图片替换层 | 通常只引用 | ABXY、LB/RB、菜单键等，原版字符串也在用 |
| `E031`–`E036` | `data/hd/global/ui/controller/hud/effecticons/immune_*.sprite` | **能改** | 品质/筛选徽章常用槽位；同码点可因 mod 替换 sprite 而显示不同图 |
| `E037` | `icon_desecrated.sprite` / `DesecratedIcon` 类槽位 | ⚠️ 单独验证 | 不是下面六个 `immune_*.sprite` 槽位 |
| `F020`–`F07D` | `kodia.ttf` 等字体 glyph | 改字体才改 | 普通字体图标，单色，吃 `ÿc`；不是全彩 sprite |

**E031–E036 的真实载体**不是字体，而是控制器 HUD 的 effecticons：

```text
data/hd/global/ui/controller/hud/effecticons/immune_fire.sprite
data/hd/global/ui/controller/hud/effecticons/immune_ice.sprite
data/hd/global/ui/controller/hud/effecticons/immune_lightning.sprite
data/hd/global/ui/controller/hud/effecticons/immune_poison.sprite
data/hd/global/ui/controller/hud/effecticons/immune_physical.sprite
data/hd/global/ui/controller/hud/effecticons/immune_magic.sprite
```

同名 `.lowend.sprite` 也应随包携带。**同样的 `E031`–`E036` 在不同成品包里显示不同图片，原因就是这些包替换了同名 `immune_*.sprite`，不是“字体里藏了彩色徽章”。**

| 码点 | sprite 槽位 | Inven 1775 原始语义 | 备注 |
| --- | --- | --- | --- |
| `U+E031` | `immune_fire.sprite` | socket `S` | 插槽/底材标记 |
| `U+E032` | `immune_ice.sprite` | magic `M` | 魔法品质 |
| `U+E033` | `immune_lightning.sprite` | rare `R` | 稀有品质 |
| `U+E034` | `immune_poison.sprite` | set `S` | 有的新版图会改成 `T`，槽位不变 |
| `U+E035` | `immune_physical.sprite` | unique `U` | 暗金品质 |
| `U+E036` | `immune_magic.sprite` | all/pink `A` | 全拾取/特殊标记 |

**出处与证据**：
- [Inven 1775](https://www.inven.co.kr/board/diablo2/5842/1775)：原始 `itemfiltericon` 附件包含上述 `effecticons/immune_*.sprite`，正文列出 `\uE031`–`\uE036` 与 socket/magic/rare/set/unique/all 的对应关系。
- [Inven 6866](https://www.inven.co.kr/board/diablo2/5842/6866)：说明 item grade mark 图片放在 `data/hd/global/ui/controller/hud/effecticons`，并指回 1775。
- [Inven 7179](https://www.inven.co.kr/board/diablo2/5842/7179?p=7)：字体包含 `E031`–`E035` 的单色兜底 glyph 和 `F020`–`F07D` 物品 glyph；这些不是全彩来源。
- [Inven 7687](https://www.inven.co.kr/board/diablo2/5842/7687?category=%ED%95%84%ED%84%B0)：成品筛选包同时带 `effecticons/immune_*.sprite` 和字体，字符串大量引用 `E031`–`E036`；字体没有 `E036` 也能显示，进一步证明 `E036` 走 sprite 槽位。

使用原则：
- `E031`–`E036` 可当标题/名称行的全彩图标使用；它们不吃 `ÿc`，不会造成颜色渗漏。
- 不要把 `E031`–`E036` 的图案含义写死为某一个 mod 的截图；**含义跟当前包内 `immune_*.sprite` 绑定**。
- 若只复制字符串、不复制 `effecticons/immune_*.sprite`，就会显示目标客户端/其他 mod 当前提供的图，或回落到字体兜底形。
- 自制 `E031`–`E036` 图案时，做法是透明 PNG → 同槽位 `SPa1` `.sprite`，并同步生成 `.lowend.sprite`；详见 `07` §10.1。UI 图标的光晕可直接画进 PNG，场景/掉落光柱类发光则走 `.particles` + `.texture`，不是 effecticon sprite。
- 验收时必须进游戏截图；字体浏览器里看到的黑白框/单色字形只能说明“有兜底 glyph”，不能证明全彩 sprite 存在。

### 2.2-pre-a 标题图标与标题/描述分层

装备标题行和描述行要分层，不要把“图标、标题、解释、数值”堆在一行：

| 层 | 推荐内容 | 规则 |
| --- | --- | --- |
| 标题行 | 品质图标 + 物品名 + 必要短标记 | `E031`–`E036` 适合放这里；零色码流优先，让引擎品质色接管名字 |
| 描述行 | 一句话解释用途/阶段 | 用描述上下文色，尽量单行，避免再放多个徽章 |
| 标注行 | 孔数、底材档、变量最大值、适用等级 | 可用字体安全符号或短文本，行尾封口 |

标题图标的工作方式：
- `E031`–`E036` 是“引用当前 effecticon 槽位图片”，不是“字体里的徽章”。文案里写码点，图由 `immune_*.sprite` 决定。
- `F020`–`F07D` 是字体图标；它可以被 `ÿc` 染色，也可能因为字形挖空露出背景而看起来像双色，但本质仍是字体 glyph。
- 标题行若混用 `ÿc` 色码和 effecticon，色码只影响文字/字体 glyph，不影响 effecticon sprite。

排版建议：
- 标题行只承担识别，不承担长解释；描述行只说“为什么值得看”。
- 多行 tooltip 在 `reversed` 下从串尾行向串首行渲染；显式色码流要每行行首自带色码，零色码流则尽量不写色码，染色条目必须在串尾 `ÿc0` 封口。
- 地面标签宽度预算里 `\ue0XX` 图片字形按 3 位计，`ÿcX` 色码按 4 位计；图标多了会挤掉真正信息。

### 2.2-pre-b Tooltip 宽度预算（ZhangMDK 实测规则）

地面装备显示框（ground label）**最大文字量 123 位**，计费：

| 内容 | 占位 |
| --- | --- |
| 半角数字/英文/符号 | 1 |
| 全角数字/英文/符号/中文/日文 | 2 |
| 颜色代码 `ÿcX` 整体 | **4**（`ÿ` 占 2 + `c`+色号占 2） |
| 图片字形 `\ue0XX` | **3** |

> 设计多行名/标注行时按此预算控行宽；色码很贵（4 位），零色码流（§1.1）在宽度上也占优。

### 2.2 零色码前缀跟随引擎品质色（前缀装饰技巧）

给品质前缀（如"精良"）换成**不带任何色码的纯字符**（文字+安全符号），它就会自动继承引擎的品质着色（普通白/无形青/带孔粉）——比硬编码颜色优雅。注意：品质标签类 key（如 `Hiquality`）**同时存在于 `item-nameaffixes.json` 和 `bnet.json`，bnet 抢显示，必须双处同改**；改 bnet 时先按 `"Key"` 定位条目块再在块内替换，全文级正则会误伤其他含同字的文本。

---

## 3. 描述染色 + 星级（item-modifiers.json）

D2R 装备属性描述默认色容易被忽视。通用做法：给**魔法属性行**上语义色 + 给重点属性附**星级标记**。

### 3.1 只染魔法属性行（关键边界）
- ✅ **染色**：`ModStr*`、`strMod*`、`Moditem*`、`*Damage`、`*Leech`、`GoldFind` 等**魔法属性行**。
- ❌ **不动**：`priceless`、`ItemStats*`（需要力量/等级、耐久度、数量、未辨识）、`Socketable` 等**结构/动态行**——这类游戏会按满足与否自动控色(红/白)，硬染会破坏动态提示。

### 3.2 染色范式（幂等可重跑）
先全清旧码再上单色 + 复位，避免叠码：
```python
import re
CODE = re.compile('\xffc.', re.S)            # ÿc + 任意一字符
def colorize(text, color):                   # color 如 '\xffc1'
    return color + CODE.sub('', text) + '\xffc0'
```
> 语义色映射（v5，DLC 色卡校正；原则=**能归类的全部归类，把"默认蓝"压到最少**；只用当前色卡已确认的码）：
>
> | 族 | 码 | 备注 |
> | --- | --- | --- |
> | 火系 | `ÿc1` 红 | 含+火焰技能 |
> | 生命·治愈·吸血族 | `ÿcT` 暗红 | 与火区分一档；含治愈药剂行 |
> | 冰系 | `ÿcU` `#85c7ff` 浅蓝 | 冰伤/冰抗/上限/吸收/冰技伤/破冰免/冻结系 |
> | 电系 + 速度族 | `ÿc9` 黄 | IAS/FCR/FHR/跑速/格挡速 |
> | 毒系 | `ÿc2` 亮绿 | |
> | **属性族**(力/敏/活/能/全属性) | `ÿcA` `#018301` 深绿 | 与毒亮绿明显分档 |
> | 魔法系 | `ÿc;` `#b001ff` 亮紫 | 含全抗 |
> | **防御·格挡族** | `ÿcP` `#ff83ff` 粉紫 | +防御/强化防御%/格挡几率；旧 `ÿcV` 现为红色，勿再当防御色 |
> | 物理伤害族 | `ÿc8` `#ffaa01` 橙 | min·max伤害/强化%/攻击特效同族 |
> | 技能·光环族 | `ÿc4` 金 | |
> | MF·GF | `ÿc7` 淡金 | |
> | 法力·未归类残量 | `ÿc3` 默认蓝 | 兜底，应尽量少 |
>
> 灰 `ÿc5` 与白 `ÿc0` 前缀在属性行中**全面淘汰**——灰在小字号下可读性差，白与基础行混淆。武器速度档（`WeaponAttack*`"攻击速度 - 快速"）与 `ItemStats*` 是基础信息行，保持默认不染。

### 3.2-bis 着色治理 SOP（两次缺漏事故的教训）

只看"有没有颜色"验收会漏掉**同色撞色**（实证事故①：物理伤害与冰系、默认色三者全是蓝，看着"已上色"实际无区分度）。只核对色表 key 会漏掉**同义重复 key**（实证事故②：护符词条走的是 DLC 新增的简化 key 族 `MaxLife`/`AllSkills`/`IAS`/`MagicFind`/`FasterRunWalk` 等，与老 `ModStr*` 系同义不同 key，色表从未收录 → 护符 tooltip 全默认色）。治理步骤：

1. **先归族再取色**：每个元素族 = 伤害+抗性+抗性上限+吸收+技能伤害+破免疫（Sunder）**全家同色**，建族清单逐 key 核对，杜绝"火抗红了、火抗上限还是兜底色"；
2. **禁止兜底到默认色以外**：未归类属性一律默认蓝，不发明第二兜底色；
3. **色表唯一落点**：染色映射只存在于染色脚本（若有多个脚本必须同步改），手改数据文件 = 下次重跑即回退；
4. **缺漏审计做"内容级语义审计"**，不要只核对色表：全量遍历词条，剥码后按中文文本正则推断"应属色族"，与实际首色码比对，diff 即缺漏清单——同义重复 key、新版本新增 key 一网打尽（注意人工复核误报：技能页名含"毒素"≠毒系）；
5. **验收抽样按"族"抽**，每族至少看一条伤害+一条抗性+一条上限，确认互相有区分度；护符/药水/咒符等**非装备物品单独抽**（它们常走另一批 key）。

### 3.3 重点星级（按重点程度上不同颜色的星）
格式：`<语义色><文字>ÿc0<星色>★ÿc0`。下表为一套**示例分档**（依 D2R 社区毕业属性优先级），可按项目调整：

| 档 | 星标 | 适用属性 |
| --- | --- | --- |
| T1 神级 | `ÿc1★` 红星 | +全技能、+职业技能、FCR、FHR、全抗、吸血、减伤%、致命打击、压碎打击、无法冰冻 |
| T2 重要 | `ÿc4★` 金星 | 单元素抗性、+主属性、+全属性、生命/法力(%)、增强伤害ED、MF、IAS、跑速、格挡 |
| T3 有用 | `ÿc9★` 黄星 | 单元素伤害、命中、防御/增强防御、GF、回血回蓝、荆棘、击退 |

---

## 3.4 地面标签颜色（D2R 内置，非 loot filter）
有孔/无形/各品质物品的**地面标签颜色**由 `_profilehd.json` 的 `TooltipStyle` 控制，是**引擎内置功能**，无需 loot filter 规则：
```
_profilehd.json → "TooltipStyle" → {
  "SocketedColor": "$FontColorLightPurple",   有孔=紫红
  "EtherealColor": "$FontColorLightTeal",     无形=青
  "DefaultColor/MagicColor/RareColor/SetColor/UniqueColor/CraftedColor"... 都可改
}
```
> 有孔+无形同时 → D2R 默认优先显示有孔色。若标签显示灰色，多半是被设成了 `$FontColorGrey`。

### 3.4.1 引擎着色公理（多轮游戏实验实证，改地面标签前必读）

1. **`ÿc0` = 恢复"该标签的默认色"，不是固定白色**。无形物品名字串尾部的 `ÿc0` 之后，名字行照样吃到引擎无形青——封口不会破坏引擎品质着色，放心用。
2. **标签默认色优先级：带孔(SocketedColor) > 无形(EtherealColor) > 品质色/白**。带孔无形按带孔色显示。
3. **引擎局限（接受它）**：带孔无形 vs 普通带孔，地面颜色无法区分（原版同款，官方论坛常年抱怨）。语义只能定成：青=无孔无形 / 孔色=带孔(悬停辨无形) / 白=普通。
4. **原版"无形显白"的常见真相**：不是 EtherealColor 没生效，而是 `FontColorSocketed` 默认淡粉 (255,170,255) **小字号下视觉≈白**——物品其实是带孔的。换醒目色（如艳粉 255,80,220）立刻可辨。同理 EtherealColor 推荐亮青 (0,255,255)。
5. 标签之间不串色；`ÿcI`/`ÿcK` 是引用 EtherealColor/SocketedColor 的动态行内码。
6. reversed 渲染下行尾悬空的**非 `ÿc0`** 色码才会渗染下一行（见 1.1）；`ÿc0` 封口 = 重置回标签默认，对暗金标签即金色，所以"显式品质前缀 + ÿc0 封口"两者兼容。

---

## 3.5 多行名字串的信息分层 + 阶段化装备解析（方法论）

给装备名字串加注解行（loot filter 增强）时，按层分工，每层只说自己的事：

| 层 | 内容 | 色码建议 |
| --- | --- | --- |
| 名字行 | 品质身份：名字+档次符号 | 暗金`ÿc4`/套装`ÿc2`/底材**无码**(随引擎) |
| 解析行 | 「核心属性亮点+阶段用法」一句话 | 行首`ÿc3`，单行 ≤24 字 |
| 标注行 | 可变属性最大值/孔数（如 `防524 孔4`） | `ÿc9`，行尾 `ÿc0` 封口 |

**解析行写法铁律（全量重写 500+ 条的教训）**：
- **按"游戏阶段"定位写"它能怎么用"**，不用终局标准贬低低级装备（早期神器≠毕业装）；机械分档可按 qlvl：1-25早期/26-50中期/51-74后期过渡/75+毕业，再按不可替代属性（如 MF、低级别对决）上调为"特定场景"；
- 数据必须出自提取的 LIVE 表（uniqueitems/setitems），**永不凭记忆写属性数字**；
- **纯中文**：禁止 prop 原码（`ac(17~17)`、`res-pois`）直接进游戏——批量生成时务必走"prop 码→中文"翻译表，并加自动断言"剥色码后不含 `[a-zA-Z(~]`"兜底；
- 禁贬低性空话黑名单（垃圾/无用/建议卖店…）与"早期装备/中期过渡"式无信息模板开头；
- 重写前把旧文案备份进档案字段（如 `old_tucao`）——它是后续返工最值钱的底稿。

## 4. 字体合并（cmap 劫持，给字库塞自定义图标）
D2R 中文字体码表狭隘。要用大量自定义图标，把图标字体的字形**合并进中文主字体**：
- **原理（cmap hijacking）**：把"未使用的 Unicode 码点"在字体里重绘为矢量图标，字符串里写该码点即显示图标。
- **UPM 缩放（关键坑）**：不同字体 Units-Per-Em 不同，直接复制字形会大小错乱。合并时按 `factor = 目标UPM / 源UPM` 等比缩放字形坐标与 metrics。（如源 UPM 256→目标 1000，factor≈3.906；源 1024→1000，factor≈0.9766）
- **图标识别**：图标字形通常宽度 = UPM（正方形）。
- **冲突区**：图标一般占数学符号/带圈数字/全角/制表符等不常用区段，作为游戏专用字体可接受覆盖。
- ⚠️ **具体"图标↔码点"映射是字体包专属的**（不同 mod 的合并字体映射不同），不要跨字体套用；以你实际字体的字形为准，用 fontTools 查 cmap + 字形宽度确认。
- 工具：fontTools.ttLib 遍历源 cmap、按宽度筛图标、缩放后写入目标 cmap（新增+覆盖）。

## 5. 百科/配方排版规范
- 数量用乘号 `×`，方向用箭头 `→`（禁用 `x` / `->`）。
- 过长说明用 `\n` 换行，续行可缩进对齐。
- 高亮材料名末尾 `ÿc0` 闭合，防止污染后续行。
- 百科里的材料名**必须与背包掉落简称一致**（避免"材料对不上"的困惑）。
